---
command: stats
args:
  - name: user
    type: user
    optional: true
---

import Argument from '../../components/Argument.astro';

Shows statistics for a user (or your own).

<p>
  If <Argument name="user" type="user" optional /> is specified, that user's
  stats will be shown. Otherwise, Engauge will show you your own stats.
</p>

#### Preview

TODO
