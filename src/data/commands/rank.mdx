---
command: rank
args:
  - name: user
    type: user
    optional: true
---

import Argument from '../../components/Argument.astro';

Displays a user's rank card (or your own).

If <Argument name="user" type="user" optional /> is specified, that user's rank
card will be shown. Otherwise, Engauge will show you your own rank card.

#### Preview

TODO
